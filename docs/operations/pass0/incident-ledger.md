# Operations Pass 0A — Incident Ledger

One row per incident found during the survey (see [evidence-log.md](evidence-log.md)
for full citations). Two incidents witnessed from both a repo's own docs and
the live Diagnostic Clank NAS incident log are merged into one row citing
both sources. Rows are grouped loosely by repo, not chronologically.
"Recurrence risk" is the surveying agent's judgment about whether the same
*failure class* (not the same bug) could recur in another fleet member,
not a claim that it will.

| ID | Date | Repo | What broke | Root cause | Fix | Recurrence risk |
|---|---|---|---|---|---|---|
| INC-001 | 2026-08-14 | watch-clank | Casio's production path never created `Event` rows; scheduled runs silently produced zero notifications | Missing `emit_events` default on the production call path | Added the parameter | Yes — invariant is call-site opt-in, not structural; a new runner can still silently under-emit |
| INC-002 | 2026-08-14/15 | watch-clank | A stale pre-migration cron launcher fired invisibly in parallel with the new systemd-timer architecture for days | Migration to the new scheduler architecture never located/disabled the old cron entry it replaced | Crontab lines commented out (not deleted) | Yes — explicitly noted as a fleet-wide pattern, citing smartphone-clank's identical prior fix |
| INC-003 | 2026-08-17 | watch-clank | A `RUN NOW` manual click on a fresh, never-baselined DB flooded 300 fabricated "new" events | First-ever run for a collector was treated identically to a scheduled run with no baseline distinction | `_auto_baseline_for_first_run` makes the first-ever run for a collector silent regardless of trigger source | Yes — later found to only cover the no-epoch case; growing an existing deployment without `--force-baseline` could replay it |
| INC-004 | 2026-08-18 | watch-clank | 47 stale/out-of-stock Citizen SKUs surfaced as NEW_REFERENCE from genuine upstream catalogue churn | Availability changes treated identically to genuinely new SKUs | Bounded per-item availability enrichment | Moderate — any Clank treating availability churn as novelty is exposed |
| INC-005 | 2026-08-19 | watch-clank | `published_at` misused as a launch-date proxy, causing false-positive novelty | Editorial timestamp field trusted as chronological/launch truth without validation | Accessory gate, staleness gate, catch-up tooling | Yes — same failure shape as smartwatch-clank's firmware-timestamp incident (INC-032) |
| INC-006 | 2026-08-21 | watch-clank | Hostile architecture audit found: Windows `os.kill` lock-liveness check could kill the wrong process; `ZERO_ITEMS` counted as a healthy status, masking 20 consecutive zero-item runs | Liveness check reused a PID without confirming process identity; health status enum treated "ran successfully" and "produced nothing" as the same state | `zero_item_warning_streak` setting added; PID-liveness issue flagged for follow-up | Yes for both — PID-liveness-as-identity is the same class of bug found independently in oem-radar/smartwatch-clank (INC-009, INC-034); zero-vs-healthy conflation is the same class found independently in 5+ repos (see cluster D) |
| INC-007 | ~2026-08-22 | watch-clank | `docker-compose.staging.yml`'s `${IMAGE_TAG:-soak-local}` silently resolved to a long-stale image predating several migrations whenever `IMAGE_TAG` was unset | Soft-default (`:-`) instead of fail-loud (`:?`) on a required deploy variable | Required `${IMAGE_TAG:?message}`; regression test forbids soft-default form fleet-wide in this repo | Yes — same convention gap plausible in any Clank's compose file using a soft default for an image tag |
| INC-008 | 2026-08-08 | oem-radar | Baseline (first-crawl) events tagged but never filtered by any downstream query; 1,875 baseline records appeared identical to genuine alerts in dashboards/metrics | Baseline tag written at collection time, never consulted at read time | Shared `EXCLUDE_BASELINE_EVENTS_SQL` predicate applied to every novelty-asserting query | Yes — identical shape to the write-tags/never-reads-them pattern found independently elsewhere in the fleet |
| INC-009 | 2026-08-23 | oem-radar (+ diagnostic-clank NAS incident `5f280abf`) | A run crashed mid-crawl leaving a `{"pid": 1, ...}` lock; every one of ~81 subsequent hourly scheduler fires concluded "the old run is still alive" and refused to start, forever, with no self-recovery | PID-liveness checks are unsound across Docker PID namespaces (every container is PID 1 to itself) | OS-level advisory file lock (`flock`/`msvcrt.locking`) instead of PID-liveness; fix imported wholesale from Free Game Tracker | Yes — same bug class independently found and fixed in watch-clank (INC-006) and smartwatch-clank (INC-034); explicitly a recurring fleet risk not yet unified into one shared primitive |
| INC-010 | 2026-08-08 | oem-radar | `schtasks /tr` could not be correctly quoted when the project path contained a space, silently registering a task that reports `SUCCESS` but runs the wrong program or no-ops | Windows scheduler-registration quoting bug, specific to a path containing a space | Switched to `Register-ScheduledTask` with separate `Execute`/`Argument` parameters | Yes, in kind — any Clank whose install path contains a space and uses the same `schtasks /tr` pattern is exposed |
| INC-011 | undated | oem-radar | A DB backup silently inherited the live DB's WAL journal-mode header, breaking read-only restores of the backup copy | Journal-mode state not reset before/after the copy | Explicit checkpoint-to-DELETE after backup | Moderate — generic SQLite-backup-portability gotcha, plausible fleet-wide |
| INC-012 | through 2026-08-14 | smartphone-clank | 165 due job executions lost (up to 95s late) by the resident APScheduler process | Single-worker `BlockingScheduler` with a misfire grace window silently dropped due jobs when one source's run overran | Migrated to one systemd timer + one-shot service per source; `is_due()` computed from DB run history, not scheduler state | Yes — any Clank using a single resident in-process scheduler with a shared worker is exposed to the same misfire-under-overrun class |
| INC-013 | 2026-08-10 | smartphone-clank | Baseline-completion gate marked a source's baseline "complete" while `process_discoveries()` silently dropped all 18 real candidates past a manufacturer filter | Two independent, uncross-checked promotion/scope gates (`WAVE1_PRODUCTION_SCOPE` vs `config.yaml::manufacturers`); only one was updated | Fail-closed startup validation (`assert_production_scope_or_refuse`), no-silent-drop logging, "baseline completion requires proof of persistence" | Yes — same dual-uncross-checked-gate shape explicitly anticipated and designed against in tablet-clank and smartwatch-clank's later promotion gates |
| INC-014 | 2026-08-06 | smartphone-clank | Repository-committed `config/config.yaml` shipped with non-Samsung collectors `enabled: true`; a local-only edit to disable them was never permanently flipped in the repo, risking silent re-enablement on any fresh checkout/extract | Local vs. repository config divergence with no drift check | `production_scope()` gating independent of the enabled flag; regression test `test_pollution_cannot_recur.py` | Yes — generic local/repo config-drift class; directly inherited as a lesson by feature-phone-clank's `scope.py` (see lineage notes) |
| INC-015 | undated ("today," found this phase) | smartphone-clank | A stale PID file referencing a dead process meant a 15-minute health-check Task Scheduler job could spawn a duplicate daemon without detecting the still-running original; two daemon process groups found running concurrently against the same production DB | Process start scripts never recorded a live PID; health-check could never detect a running instance | (fix not detailed in this pass beyond stopping the two orphaned daemons) | Yes — generic "no PID/liveness bookkeeping at process-start time" class |
| INC-016 | undated, "confirmed reproducing at least twice" | smartphone-clank | A `wave1_baseline_state` table repeatedly appeared in production with zero rows | `main.py db upgrade`'s implementation was literally `Base.metadata.create_all()`, so a stray model import in the shared working tree could silently add schema to production | Alembic made the sole schema-mutation authority; three separate entrypoints refuse to start on schema-behind-head | Yes — explicitly flagged as likely to recur again since the underlying scheduled process keeps running against a live, shared working tree |
| INC-017 | 2026-08-xx (Hetzner cutover) | smartphone-clank | A fresh Linux checkout broke on deploy: missing `alembic` in `requirements.txt` (installed out-of-band on the Windows dev machine) and an unanchored `.gitignore` rule silently excluding tracked reference YAML | Both defects were invisible on the dev machine and only surfaced on a genuinely clean remote checkout | Fixed and committed before redeploying (no uncommitted Hetzner-only patches) | Yes — "works on my machine" drift is generic; the fix pattern (clean-checkout verification before trusting a deploy) is the mitigating practice |
| INC-018 | 2026-08-18, unresolved as of doc | feature-phone-clank | Last observed scheduled HMD runs on Hetzner failed with `ReadTimeout`; whether the timeout is still ongoing was not re-verified across at least two work sessions | Scheduled-run failure not actively monitored/re-checked after the catastrophic-zero guard correctly prevented data corruption | Catastrophic-zero guard held prior state correctly; the underlying timeout itself was left as an open follow-up | Yes — "the safeguard worked, so nobody circled back to check if the source is actually still broken" is a generic monitoring gap |
| INC-019 | undated | feature-phone-clank | A product's SKU changed once and then stayed changed, but the identity-anomaly check kept comparing against a field set once at creation and never updated, firing a second spurious notification for the same real-world change | Anomaly detection compared against a stale creation-time snapshot rather than the current record on each run | Event creation gated on `is_new_obs` | Moderate — generic "comparison baseline never refreshed" class |
| INC-020 | undated | chinese-tech-wire | hkepc, xfastest, and PTT sources all became non-functional (HTTP 402/403/500) | Anti-bot/access blocks and, for PTT, apparent gateway overload — verified source-side | Sources marked `DISABLED`/`BLOCKED` in a policy dict; historical data preserved | Yes, in kind — same found-vs-new-zero-streak detection shape independently built in korean-tech-wire (see cluster D) |
| INC-021 | ~2026-08-10 through 2026-08-18/19 | korean-tech-wire | A 30-minute systemd timer ran full 5-source cycles instead of the documented 2-hour cadence — roughly 4x the intended request rate sustained for over a week | `run_soak`'s due-check treated the whole fleet as one AND-gated unit; one permanently-failing source (SK hynix) kept the gate permanently "due" | Per-source due evaluation from persisted run-health rows, plus exponential failure backoff; 15 new regression tests including a fake-clock restart-survival test | **Yes, generic** — a whole-collection AND-gate over per-item due/health state is a pattern any Clank using a single shared gate across independent sources could reproduce |
| INC-022 | since 2026-08-10, diagnosed 2026-08-19, unresolved | korean-tech-wire | SK hynix Korea (the fleet's original sole production source) collected zero new articles for the entire ~8.5-day soak | AWS ALB-level IP/ASN block against the Hetzner egress IP, reproducible on every endpoint | Not recovered; automatic backoff to a 24h retry ceiling instead of continued hammering; surfaced via the health command's schedule output | Yes, in kind — any collector hosted from a shared datacenter egress IP is exposed to the same infrastructure-level block class |
| INC-023 | found before promotion | korean-tech-wire | Samsung Newsroom Korea showed perfect reliability (403/403 successful runs) but zero of 19,000+ discovered items were ever rejected, because the editorial filter had no branch for that source | Reliability metrics alone were treated as sufficient promotion evidence; no relevance/filter coverage check | REWORK decision — not promoted; filter gap identified before going live | Low-moderate — a genuine near-miss, caught before harm; worth noting reliability != readiness as a general pattern |
| INC-024 | caught during testing, undated | semiconductor-intelligence | A rollback in a discovery/suggestion failure path would have silently discarded a promotion that had *just been committed* in the same transaction | Promotion commit and the subsequent discovery/suggestion attempt shared a rollback boundary | Promotion committed before either subsequent step is attempted | Moderate — generic transactional-ordering hazard around any promotion/commit sequence |
| INC-025 | 2026-08-03 | semiconductor-intelligence | A populated-database checkpoint was accidentally repointed at an empty smoke-test database | A disposable smoke-test run overwrote `semintel.config.json`, which production config also read from | Restored portable relative config; added a packaging regression test | Yes — generic "test/smoke tooling writes into a path production config also reads from" class |
| INC-026 | observed, not treated as a defect | semiconductor-intelligence | The Alembic head recorded in a prior audit differed by one migration from the actual current head at re-audit time | Documentation snapshot drifted from actual state between audits | Noted as an observation, not remediated as a defect | Low — self-correcting via re-audit, but illustrates that even internal audit snapshots can drift |
| INC-027 | 2026-08-22, operator-verified | fleet-wide (oem-radar, smartwatch-clank, feature-phone-clank simultaneously) | An interactive root redeploy ran `git stash -u`/`git stash pop`; untracked `logs/` directories were recreated `root:root`, and cron shell redirects failed *before* collector execution. Scheduler invocations existed, zero application runs materialized, no failure records were written. The outage stayed silent for ~36 hours across three Clanks at once | Root-privileged redeploy interacting badly with untracked runtime directories inside checkouts | Codified as the canonical fleet lesson: `SCHEDULE_EXPECTED ≠ SCHEDULER_FIRED ≠ PROCESS_STARTED ≠ RUN_MATERIALIZED ≠ RUN_COMPLETED ≠ OUTCOME_RECORDED` (ADR-0008); registered in the Golden Incident Corpus | **Yes, explicitly fleet-wide** — this is the single most severe and broadest-blast-radius incident found in this survey; flagged as a hazard class, not a one-off |
| INC-028 | 2026-08-24 | oem-radar | Three legitimate hourly executions fired, started, and completed successfully with zero sources crawled (correctly due-gated) and wrote no `crawler_runs` row; a monitoring tool inferred a materialization gap from "fired + started + no record," which was a false positive | A legitimately empty natural cycle was indistinguishable, to the monitor, from a failure to materialize a record | Introduced a `materialization_policy` enum (ALWAYS/WHEN_WORK_ATTEMPTED/OPTIONAL/UNKNOWN) and positive `NO_WORK_DUE` evidence, never inferred from absence | Yes — any monitor that infers failure from "no row written" without a positive no-work signal is exposed to the same false-positive class |
| INC-029 | 2026-08-22/23, operator-verified | fleet-wide (agent-performed) | `docker volume rm` run against pattern-inferred volume names destroyed two real production volumes; "staging" appearing in a name provided zero evidence the volume was actually disposable | An agent trusted a naming-pattern guess instead of resolving actual volume identity before a destructive mutation | Codified an 8-step contract: DISCOVER → RESOLVE ACTUAL IDENTITY → CLASSIFY → PROVE BACKUP → DISPLAY EXACT TARGET → EXPLICIT AUTHORISATION → MUTATE → VERIFY | **Yes, explicitly fleet-wide** — directly connects to INC-041/INC-042 below (the two NAS DB-deletion incidents); this is a repeated failure class, not a one-off |
| INC-030 | 2026-08-29 | tablet-clank | A fleet-wide "timer not found" sweep covering NAS/WSL/Windows reported the Hetzner scheduler as absent; it was in fact live and had been since 2026-08-26 | The sweep never checked the one host where the timer was actually running | Later, separate Hetzner-specific check re-confirmed the true live state | Yes — a scheduler-truth audit that only checks some hosts can produce a false negative just as easily as a false positive |
| INC-031 | ongoing as of 2026-08-29 | tablet-clank | Hetzner's live deployment still ran the Wave-1 three-source production allowlist while the repository's `main` already reflected the Wave-3 four-source allowlist | Deploy lagged a legitimate repo change; no automatic re-sync mechanism exists | Documented as known drift, not yet resolved at time of survey | Yes, in kind — same repo-ahead-of-deployed-host class documented fleet-wide in clank-architecture's Risk Register/Fleet Law 9 |
| INC-032 | 2026-08-2x | tablet-clank | An operator manually aborted a Windows soak campaign mid-run (cycle 2/12) to relocate it to a NAS; `CTRL_C_EVENT` did not reliably reach the app's interrupt handler because the console was shared with unrelated processes, forcing a forced-kill fallback | Windows console signal delivery is unreliable when a long-running process shares a console with unrelated ancestor processes | Operator confirmed no in-flight I/O via JSONL/file mtimes before force-killing; campaign DB verified intact; the 2 completed cycles explicitly excluded from later promotion-counting evidence; the underlying signal-delivery gap itself was left as a documented workaround, not fixed in code | Yes, in kind — any other Windows-hosted long-running Clank process sharing a console with unrelated processes has the same signal-delivery gap |
| INC-033 | 2026-08-30 (adjudicated) | smartwatch-clank | A collector injected 23 simultaneous false `FIRMWARE_RELEASED` high-confidence events from a single editorial timestamp touch, caught by live soak evidence before it reached production | An editorial `updated_at` timestamp was used as the field driving change classification, matching the same failure shape as watch-clank's `published_at` misuse (INC-005) | Promotion blocked; soak clock will restart fresh at the first post-fix natural cycle once the field is no longer used this way | Yes — same "timestamp-shaped value mistaken for chronological truth" class found independently in watch-clank |
| INC-034 | resolved 2026-08-30 | smartwatch-clank | The soak deploy path correctly propagated a proxy config variable to its container; the production deploy path (a separately-maintained, initially-untracked wrapper script) did not, silently disabling the proxy in production | Two independently-maintained deploy wrapper scripts for two lanes had to be manually kept in sync and drifted | Untracked wrapper brought under version control as the canonical tracked source; explicit "keep the two in sync" comment added | Yes — generic dual-maintained-deploy-script drift class, same shape as watch-clank's repo/systemd drift (INC-007-adjacent) |
| INC-035 | 2026-08-19 | smartwatch-clank | A type collapsed "collector's own tier" and "the current invocation's scope," so once the new experimental soak timer went live it began silently re-running the four production-tier Samsung collectors on its own independent 2-hour cadence, in addition to their real production cron | An `EXPERIMENTAL` scope check on `CollectorRegistry.selected()` had been written to mean "return every collector regardless of tier," inherited unchanged from before multi-OEM expansion | New `RunScope` enum decoupled from `CollectorTier` | Yes — explicitly flagged as a recurrence risk for any Clank reusing a similar tier/mode enum pattern; nothing corrupted only because both schedules shared one lock |
| INC-036 | 2026-08-23 | smartwatch-clank (+ diagnostic-clank NAS incident, agent-performed) | Destructive volume deletion destroyed the live database including all observations newer than the newest backup; restored from a ~4-day-stale backup | Same destructive-mutation-without-identity-verification root cause as INC-029/INC-041 | Restored from backup; explicit continuity/epoch bookkeeping added ("restoration does NOT imply continuity"); soak clock not reset by the incident itself, affected lanes instead report UNKNOWN/NOT-YET-MATURE | **Yes, explicitly fleet-wide** — see INC-029, INC-041 |
| INC-037 | 2026-08-30 | free-game-tracker (NAS) | A DSM (Synology) scheduled task showed healthy/enabled with a plausible-looking "Next Trigger" for 2.4 days while producing zero real executions | A relative path stored without a leading slash in one of three sibling task-config fields — visually indistinguishable from a working task in the DSM UI | Root cause identified and corrected | Yes — explicit DSM-specific UI gotcha that could recur on any Clank scheduled via the same DSM task mechanism |
| INC-038 | 2026-08-27 | chinese-tech-wire (NAS) | Neither Hetzner nor NAS was ever formally an approved "production" tier for CTW — "Hetzner canonical" was informal convention, never governance-approved, discovered during cutover-readiness review | Promotion/tier authority was assumed rather than verified | Findings recorded; formal tier decision still pending at time of incident closure (marked PARTIAL) | Moderate — a governance/documentation gap rather than a code defect, but directly matches cluster G's dual-gate-drift theme |
| INC-039 | 2026-08-27 | fleet-wide (ClankLift census) | SSH access gaps to both Hetzner and NAS forced reliance on stale documentation; several fleet facts (own deployed revision, tablet-clank's presence, CTW's migration status) were unconfirmable until access was restored | Host access itself, not application code, was the single point of truth-verification failure | Access restored; findings reconciled | Moderate — illustrates that "remote host truth" claims are only as good as current access, a structural dependency across the whole fleet |
| INC-040 | 2026-08-27 | diagnostic-clank (NAS, self) | Diagnostic Clank's own deployed NAS instance sat 8 days / 1 commit stale versus `origin/main`, predating a real feature, and nobody noticed until a deliberate census | No automatic re-deploy-freshness check on the diagnostic tooling's own instance | Found and flagged during the census | Yes, in kind — same "repo ahead of deployed host, unnoticed" class as INC-031 and Fleet Law 9's KTW/SemInt violators |
| INC-041 | 2026-08-23 | feature-phone-clank (NAS, agent-performed) | An agent ran a destructive volume-deletion action against the real production DB volume during remediation, trusting a naming-pattern guess instead of a read-only listing first; **total, unrecoverable data loss — no backup existed** | Same destructive-mutation-without-identity-verification root cause as INC-029/INC-036 | None possible — total loss; the incident is part of what motivated ADR-0009's 8-step destructive-safety contract | **Yes, explicitly fleet-wide and severe** — the most severe single incident found in this entire survey; occurred twice in one week (see INC-036) |
| INC-042 | 2026-08-18 | chinese-tech-wire (NAS, desktop-specific) | A packaged desktop app's subprocess invocation used its own bootloader as the interpreter | Packaging/subprocess-invocation defect specific to the desktop-app build | Resolved | Low — appears specific to diagnostic-clank's own desktop packaging, not the scraping Clanks' scheduler/deploy architecture |
| INC-043 | 2026-08-18 | free-game-tracker (NAS) | A real news item (Helldivers 2 PS Plus) was correctly discovered but never posted to Discord | The delivery path was missing/misconfigured while collection itself succeeded — collection health and delivery health were not tracked as separate axes | Resolved; directly cited as motivating evidence for ADR-0007's collection-health-vs-delivery-health split | Yes — same health-axis-conflation class independently found in feature-phone-clank's deliberate `delivery_health`/`source_health` split (see cluster C) |
| INC-044 | 2026-08-18 | watch-clank (NAS) | Two concurrent Docker containers both ran as full database writers against the same DB | Container-local lock file did not prevent a second container (different host-level process, same logical service) from also acquiring writer status | Resolved | Yes — containerization breaks single-host/single-process lock assumptions generally, not just for watch-clank |
| INC-045 | 2026-08-18 | oem-radar (NAS) | A DSM Task Scheduler recurrence was misread as hourly when it was actually configured daily | DSM UI ambiguity in how recurrence is displayed | Resolved | Yes — same DSM UI-gotcha class as INC-037, independently hitting a second Clank |

**Row count check**: 45 incident rows above (INC-001 through INC-045), all with dates, root causes, fixes where known, and an explicit recurrence-risk judgment.

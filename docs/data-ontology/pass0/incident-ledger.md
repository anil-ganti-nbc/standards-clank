# Pass 0A — Incident Ledger

One row per incident found across all six surveys (see
[evidence-log.md](evidence-log.md) for full citations). Where the same
incident was independently cited from two directions (e.g. an oem-radar
incident also referenced by diagnostic-clank), it is listed once with both
sources noted. **Confirmed** means at least one survey read a primary
source document describing the incident directly. **Unconfirmed/stub**
means only a seed-stage lesson record exists with no primary source
available in the surveyed clone — included per the mission's instruction
to actively mine for incidents, but must not be treated as fact without
further verification.

| ID | Repo | Date | What happened | Root semantic mistake | Remediation | Recurrence risk | Cluster | Status |
|---|---|---|---|---|---|---|---|---|
| INC-01 | watch-clank | 2026-08-11 | Baseline-era specialist leads displayed with no freshness distinction; a 5-month-old rumor could look as fresh as same-day | "Editorial freshness" didn't exist as a concept downstream of `is_baseline` | `app/services/freshness.py` built | Recurred in different form (INC-03, INC-06) | A | Confirmed |
| INC-02 | watch-clank | 2026-08-17 | A genuinely recent launch (published 10 days pre-baseline) permanently silenced by an unconditional baseline guard | Baseline suppression never consulted already-captured `published_at` evidence | `classify_baseline_product_freshness`, empirically-tuned 72h window | Scoped to `is_new_watch` only; other transition types remain unconditionally silent during baseline | A/F | Confirmed |
| INC-03 | watch-clank | 2026-08-17 | ~1045 real `NEW_REFERENCE` events fired in one run on a never-baselined local DB | No burst-context communicated downstream; correct detections read as independent stories | `probable_catalogue_backfill` annotation (additive only) | **LIVE, UNRESOLVED**: Hetzner production `operational_epochs` table is empty; baseline protection depends entirely on a manually-passed `--force-baseline` flag per source. Any future source onboarded without it hits the same unprotected mechanism with live Discord webhooks. Three remediation options proposed, none implemented. | F | Confirmed |
| INC-04 | watch-clank | 2026-08-18 | 47 real `NEW_REFERENCE` events from genuine catalogue churn; 45/47 confirmed sold out; `availability_status` NULL for all 47 at discovery | Discovery path never carried inventory data; a source `market_status` field was captured but unused | Bounded per-product enrichment fetch for new items only, non-baseline runs; `market_status` kept as context only (proven unsafe as a filter — a Phase-Out SKU had 7 units in stock) | Motivated the fleet's `EventReview`/QC system | B/G | Confirmed |
| INC-05 | watch-clank | 2026-08-12 | UK launch of an already-known US reference invisible; no concept of "same reference, new region" existed | Regional identity/coverage gap (`TRANSITION_DETECTION_FAILURE`) | `NEW_REGION` transition type added, cross-currency guard against false `PRICE_CHANGE` | Regional identity is still admittedly provisional fleet-wide (see regional-variant-identity cluster) | C | Confirmed |
| INC-06 | watch-clank | 2026-08-19 | Batch of real false positives/misses: accessory-only blog post → false `NEW_REFERENCE`; `notify_correlation` had no staleness check (142-day-stale lead nearly sent as fresh); an 11-month-old RESTOCK sat at equal QC priority to new items; a REACTIVATED catalogue tag needed explicit surfacing | `published_at` conflated with launch authority at scale (23-product cluster sharing one identical bulk-republish timestamp) | Permanent policy: "`published_at` is not launch authority"; multiple point fixes | Same class as INC-01/02 | A | Confirmed |
| INC-07 | oem-radar | 2026-08-08 | All 1,875 baseline-era change events counted as ordinary alerts by dashboard/summary/feedback metrics | Correctly-created `baseline` meta flag never consulted downstream | Shared `EXCLUDE_BASELINE_EVENTS_SQL` predicate | **Same failure shape as INC-01/watch-clank, independently discovered, no cross-citation between repos.** Also cited by diagnostic-clank (DB-002) as a fleet-wide pattern. | A/F | Confirmed |
| INC-08 | oem-radar | undated | Coarse `model_key` fallback merged two genuinely different SKUs sharing a model_key+tier word (e.g. 64GB vs 32GB variant) | Coarse candidate-surfacing key mistaken for a confident-match key | Vendor-SKU-disagreement guard | **Recurred independently** in the Evidence Fusion identity-linking code months later (same bug class, caught by regression test before shipping) | C | Confirmed |
| INC-09 | oem-radar | Stage 11 → 11.1 | Evidence observations (44.6% of all "alerts") written into the canonical product-alert table, burying real product-change signal (filled 300/300 dashboard slots) and corrupting signal/noise metrics | "An official source lists this" treated as the same claim type as "a tracked product changed" | Schema v7 `evidence_events` table; shared `PRODUCT_CHANGE_TYPES`/`EVIDENCE_CHANGE_TYPES` boundary definition | Cited independently by diagnostic-clank as a fleet-wide pattern (baseline/observation-vs-fact conflation) | D | Confirmed |
| INC-10 | chinese-tech-wire | version-labeled (V0.5.3) | ~3,121 real leads existed; alert-eligibility thresholds made zero ever reach ACTIONABLE — total alert silence; separately, `notified=True` set even when Discord send failed | Lifecycle status conflated with alert-eligibility as one gate; "attempted" conflated with "confirmed" for the `notified` flag | Alert eligibility separated from lifecycle; structured `DiscordSendResult`; `notified` only set after confirmed send; `lead_notifications` ledger | Generic "attempted ≠ confirmed" pattern worth checking elsewhere in the same repo | A/D | Confirmed |
| INC-11 | korean-tech-wire | 2026-08-10 to 2026-08-19 | A fleet-level "due" fact derived as AND over per-source due-states; one permanently-failing source kept the entire fleet permanently due, producing ~4x intended request rate for over a week | Aggregate state computed by requiring all per-item states to agree, instead of per-item evaluation | Strictly per-source due-ness from persisted history, explicit backoff | Generic "fleet state derived incorrectly from per-source state" pattern — worth checking elsewhere | F | Confirmed, dated |
| INC-12 | korean-tech-wire | confirmed 2026-08-19 | Samsung Newsroom Korea had no editorial-filter branch; 100% of 19,344 references defaulted to "accepted" | Absence of a filter branch was indistinguishable at runtime from "genuinely no low-value content" | None yet — source lifecycle downgraded to REWORK | Any future source that forgets its filter branch is exposed to the same silent-fallthrough | B | Confirmed |
| INC-13 | feature-phone-clank | 2026-08-23 | Destructive Hetzner volume deletion destroyed all prior observation history; no backup existed | No backup layer existed before this loss | `core/continuity.py` epoch/DATA_LOSS registry; new epoch declared, prior epoch identity permanently recorded as unknown, not fabricated | Underlying "no snapshot policy" risk class flagged as outside this pass's remit | F | Confirmed |
| INC-14 | feature-phone-clank | inherited, pre-dates repo | (See INC-16, smartphone-clank) — cited here as the reason feature-phone-clank's production-scope allowlist exists from inception | Cross-Clank inherited lesson | `config/scope.yaml` allowlist + production/experimental run split, built in from day one | Prevented in this repo by design | F | Confirmed (cross-repo citation) |
| INC-15 | feature-phone-clank | pre-merge | `IDENTITY_ANOMALY` compared each run's SKU to a fixed-at-creation `model_number`; a product whose SKU changed once then stayed kept re-triggering every run | "SKU differs from creation-time value" (always true after one change) conflated with "a new change just happened" | Gated on `is_new_obs`; regression test | Contained — pattern consistently guarded elsewhere in `process_run` | C | Confirmed, fixed |
| INC-16 | smartphone-clank | investigated 2026-08-06 | Non-Samsung generic collectors shipped `enabled: true` with no validation gate; marketing sentences parsed as `model_number` identity; 73 garbage Device rows | Absence of identity-plausibility validation at the discovery→canonical boundary, compounded by absence of a production-scope gate independent of config flags | Structural, config-flag-immune `production_scope()` allowlist; release-blocking regression tests | Fix is Samsung/production-scope-specific; new OEM collectors still rely on per-OEM validators, not a generic plausibility gate (admitted: "Motorola and Honor normalizers have real edge-case bugs") | C | Confirmed |
| INC-17 | smartphone-clank | 2026-08-10 | Production `manufacturers` allowlist wasn't extended for Motorola; every Motorola discovery silently dropped, yet baseline tracker still marked itself complete; next cycle would have fired 18 false "new device" alerts | Baseline "completeness" defined by run-mechanics succeeding, not by proof anything was persisted | Baseline completion now requires `new+updated+resighted &gt; 0` whenever `valid &gt; 0`; fail-closed startup validation; regression test | General "completion flag derived from mechanics, not from the write happening" pattern — worth checking elsewhere | F | Confirmed |
| INC-18 | smartphone-clank | fixed 2026-08-10 | 129 historical `Alert` rows (2026-08-02 to 2026-08-10), all `discord_message_id IS NULL` — zero represent a real Discord delivery, yet indistinguishable from real alerts by a naive count query | "We decided to alert" conflated with "we successfully alerted," one table, no delivered/attempted distinction | `WebhookDelivery` introduced as source of truth for delivery decisions; `Alert` redefined to mean exactly "delivered"; historical rows left in place, undeleted, with a documented caveat | Generic "did we do X" vs "did X succeed" pattern — not found elsewhere in this pass, but no schema-wide test enforces the distinction | D | Confirmed |
| INC-19 | smartphone-clank | ongoing, unresolved per repo's own docs | A shared, live working tree meant an independent scheduled process's `create_all()` call picked up a new SQLAlchemy model and applied it to the *production* schema — twice, confirmed via SHA-256 mismatch | Schema-mutation authority not centralized; multiple `create_all()` call sites could each independently promote a model to production schema | Alembic established as sole schema-mutation authority (`schema_guard.py`) | Explicitly stated by the repo's own docs as "still completely real and still unresolved" | E (borderline) | Confirmed, **unresolved** |
| INC-20 | smartwatch-clank | discovered/adjudicated 2026-08-30 | A collector wrote a Zendesk **editorial** `updated_at` into the **hardware-semantic** `firmware_version` field; a site-wide help-centre touch produced 23 simultaneous false `FIRMWARE_RELEASED` events sharing one timestamp | A timestamp-shaped value used as if it were a change-sensitive semantic value | Documented (two options); **not yet implemented** — source BLOCKED from production | No repo-wide invariant/test found preventing any *other* field from the same class of mistake | A/F | Confirmed, **fix pending** |
| INC-21 | smartwatch-clank | 2026-08-23 | Destructive volume deletion destroyed the live DB; restored from a 2026-08-18 backup; ~5 days of observations permanently lost | (Correctly handled — included as a positive example) | `core/continuity.py` append-only, content-hashed epoch ledger; explicit doctrine "absence inside this window is never zero and never novelty" | `SMARTWATCH_CLANK_HOST_ID` pinned to prevent ephemeral hostnames fabricating spurious gap records | A/F | Confirmed, handled correctly |
| INC-22 | semiconductor-intelligence | pre-dates semi_intel (Signal Radar era) | `_find_story` created/joined a story from one matching shared entity with no clustering conservatism; "Jensen Huang" and an unrelated entity pair became top-scored "stories"; a later hard-block filter was additive-only, never retroactively purged | Identity/dedup conflated with canonical-fact promotion; no conservatism in single-signal clustering | `SignalEntityMention` proposal layer + `SignalIndependenceGroup` conservative clustering — a full architectural redesign, not a patch | Direct motivation for rebuilding the entire candidate layer rather than importing Radar's `stories` table as canonical | C/D | Confirmed |
| INC-23 | semiconductor-intelligence | v1.0.0 Phase 2 | Independence-group "origin" selection used row-insertion order (`id`) instead of `posted_at`, silently mispicking origin whenever collection order and publish order diverged | Row-insertion order (a database artifact) mistaken for chronological origin | Fixed; regression test names the exact failure mode | Same class as smartwatch-clank's coros incident (timestamp/ordering-shaped values) — independently found | A/D | Confirmed, fixed |
| INC-24 | semiconductor-intelligence (oem_radar sub-system) | — | `stale_run_threshold_minutes` config existed with zero consumers; a job killed mid-run left an `OperationalJobRun` row stuck at RUNNING forever, invisible to the operator | An unconsumed threshold setting silently meant "nothing checks this" | Flag as `degraded` once past threshold; the stuck row's own status deliberately never rewritten (stays honestly RUNNING) | Generic "dead config assumed to be doing something" pattern | B | Confirmed |
| INC-25 | diagnostic-clank / oem-radar | Stage 11 era | (Same incident as INC-09, cross-cited independently from the diagnostic-clank corpus as `DB-002`) | — | — | — | F | Confirmed (cross-citation) |
| INC-26 | diagnostic-clank / oem-radar NAS canary | 2026-08-16 | An agent's single-observation claim ("scheduler fired once at :35") was treated as proof of a recurring hourly cadence; owner later established it was actually once-daily | A single successful observation elevated directly to a canonical cadence claim with no corroboration | Verification checklist drafted (`PARTIAL` — not confirmed rolled into production acceptance) | Explicit fleet-wide consequence recorded: scheduler acceptance requires multi-interval observation | A/D | Confirmed |
| INC-27 | diagnostic-clank (multiple Clanks) | — | Failed or zero-result collector runs allowed to advance "healthy" absence/baseline counters, scored as success | Unknown/failed state conflated with a definite negative fact ("catalogue legitimately empty") | Health thresholds + early return (oem-radar); `blocked_zero_result` (feature-phone-clank, see INC-A below); `unexpected_zero_is_failure` (smartwatch-clank) | Confirmed fleet-wide anti-pattern via shared `FailureClass.CATALOGUE_COLLAPSE`/`CHALLENGE_PAGE_AS_ZERO` enums | B/G | Confirmed (primary source not in clone) |
| INC-28 | diagnostic-clank / feature-phone-clank | — | (Sibling of INC-27) catastrophic-zero risked flowing through as a normal empty success | Same conflation as INC-27 | `blocked_zero_result` status; pipeline never invoked on blocked runs; dedicated regression tests | Direct sibling of INC-27, shared contract | B | Confirmed |
| INC-29 | diagnostic-clank / oem-radar (BANKAI) | undated | Reportedly 0/50 qualifying stories recalled despite operationally-healthy collectors | Operational health (collectors ran, no errors) conflated with intelligence/recall quality | None confirmed — case marked `OPEN`, root cause `UNKNOWN` | Standing lesson: "intelligence health and gold-set recall must not be collapsed into operational health" | D/G | **Unconfirmed — thin evidence, treat as unverified** |
| INC-30 | diagnostic-clank (fleet-wide) | — | A local directory sweep was treated as the canonical fleet inventory, making Tablet Clank appear absent from the fleet | Local-workspace-presence conflated with canonical-registry membership | `ClankRegistry` + documented canonical-inventory doctrine | Explicitly still an ongoing operational temptation per the doctrine's own warning ("do not delete unknown systems to make fleet health appear green") | C | Confirmed |
| INC-31 | diagnostic-clank / clank-fleet adapter (smartphone-clank) | — | `last_run()` ordered candidate rows by row `id` (a UUID, sorts lexicographically) instead of `finished_at`, risking a false-STALE or false-HEALTHY read | An incidental identifier property (UUID sort order) mistaken for the actual temporal/observation-strength property | Fixed; adversarial regression fixture (`uuid_trap_db`) | Named "Motherclank M1.5 adapter truth-cleanup" — implies the bug class recurred across multiple adapters | D | Confirmed, fixed |
| INC-32 | diagnostic-clank / clank-fleet adapters (watch-clank, smartphone-clank) | — | Native per-Clank status vocabularies fell through to UNKNOWN before an explicit mapping existed | Risk being guarded against: unknown-native-status silently upgraded to a false "healthy" | Explicit `_map_run_status()` vocabulary table; regression tests assert unmapped values must stay UNKNOWN and can never resolve to OK | Shared contract now reused across adapters; a *new* adapter re-implementing its own mapping could reintroduce the bug | B | Confirmed |

## Unverified stubs (named, but no primary source available in surveyed material)

These appeared only as seed-stage lesson records with explicit "do not invent" caveats in their own text. Listed for completeness per the mission's incident-mining instruction; **do not treat as confirmed incidents**.

- Beelink ME Pro HX 470 miss (oem-radar) — `diagnosticbench/cases/SEED_FIRST_10.yaml` (`L-OEM-004`)
- Stale `.app` build vs. canonical bundle confusion (feature-phone-clank) — `DB-010.yaml`/`L-PHONE-001`, evidence status `HISTORICAL_L_INCOMPLETE_EVIDENCE`
- Helldivers 2 PS Plus catalogue miss (free-game-tracker) — `L-FGT-001` only
- Samsung traversal success / zero extraction (smartphone-clank) — `L-SMART-001` only

## Borderline / flagged as likely out-of-domain

Included in the survey output because the mining instructions asked for
broad recall, but these read as operational/infrastructure/deployment
concerns more than data-meaning concerns — listed here rather than in the
main table so Pass 0B can quickly deprioritize them:

- Watch-clank SQLite writer contention under timer fan-out (concurrency/infrastructure)
- Feature-phone-clank/smartphone-clank/tablet-clank build-artifact and packaging issues (itel name-extraction parsing bug; stale `.app` bundle)
- OEM Radar NAS dual-host delivery-authority ambiguity during a migration window (delivery/operational)
- `schtasks` quoting bug silently no-op'ing a scheduled task while reporting SUCCESS (operational; included only because "reported success" is a status-shaped value mistaken for a semantic one — same *family* as INC-26, but the underlying fact is operational, not data-truth)
